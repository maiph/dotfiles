#!/usr/bin/env bash

set -e

# if ! command -v docker &>/dev/null; then
#     brew install --cask docker-desktop
# fi

# if [ ! -f "$XDG_DATA_HOME/zsh/completions/_docker" ]; then
#     docker completion zsh >"$XDG_DATA_HOME/zsh/completions/_docker"
# fi

exit 0
